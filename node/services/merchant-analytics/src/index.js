const {connectMongo}=require('./external/db/mongo');const Consumer=require('./external/kafka/consumer');const AnalyticsRepository=require('./internal/analytics/repository');const AnalyticsService=require('./internal/analytics/service');const {createApp}=require('./api/rest/app');const {createRouter}=require('./api/rest/routes');
(async()=>{const db=await connectMongo();new Consumer(db).start();const svc=new AnalyticsService(new AnalyticsRepository(db));createApp(createRouter(svc)).listen(process.env.PORT||3002,()=>console.log('merchant-analytics listening'))})().catch(e=>{console.error(e);process.exit(1)});
