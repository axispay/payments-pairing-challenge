const express=require('express');const makeHandlers=require('./handlers');function createRouter(services){const r=express.Router(),h=makeHandlers(services);r.get('/health',h.health);r.post('/accounts',h.createAccount);r.get('/accounts/:id',h.getAccount);r.post('/accounts/:id/debit',h.debit);r.post('/payments',h.createPayment);r.post('/api/transfer',h.transfer);r.use((err,req,res,next)=>res.status(err.status||500).json({error:err.message}));return r}module.exports={createRouter};
